<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/favorite_navigation"
    app:startDestination="@id/favoritesFragment">

    <fragment
        android:id="@+id/favoritesFragment"
        android:name="com.example.aniverse.FavoritesFragment"
        android:label="fragment_favorites"
        tools:layout="@layout/fragment_favorites" >
        <action
            android:id="@+id/action_favoritesFragment_to_favoritesDetailFragment"
            app:destination="@id/favoritesDetailFragment" />
    </fragment>
    <fragment
        android:id="@+id/favoritesDetailFragment"
        android:name="com.example.aniverse.FavoritesDetailFragment"
        android:label="FavoritesDetailFragment" />
</navigation>